<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ghm.giftcardfleamarket.item.mapper.ItemMapper">

    <resultMap id="itemResultMap" type="Item">
        <result property="name" column="name"/>
        <result property="price" column="price"/>
    </resultMap>

    <select id="selectItemsByBrand" parameterType="map" resultMap="itemResultMap">
        SELECT name, price FROM item
        WHERE brand_id = #{brandId}
        ORDER BY id ASC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="itemTotalCount" parameterType="Long" resultType="int">
        SELECT count(*) FROM item
        WHERE brand_id = #{brandId}
    </select>

</mapper>